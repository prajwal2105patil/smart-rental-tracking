# Phone mode serves https and proxies the API through the same origin (see
# vite.config.ts). A relative base is what makes that work: an absolute
# http:// URL would be blocked as mixed content on an https page.
VITE_API_URL=/api
